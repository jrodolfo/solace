{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "target": "esnext",
    "lib": ["esnext", "dom"], // Add necessary modern libraries
    "skipLibCheck": true,     // Temporarily disable type checking in `.d.ts` files
    "types": []               // Ignore global type definitions from node_modules
  }
}
