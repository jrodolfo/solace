services:
  mysql:
    container_name: solace-mysql
    image: mysql:latest
    environment:
      MYSQL_DATABASE: solace
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: verysecret
      MYSQL_USER: myuser
    ports:
      - '3307:3306'
    volumes:
      - './mysql-data:/var/lib/mysql'
